<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity
            android:name=".Bgroup"
            android:exported="true" />
        <activity
            android:name=".Ambulance"
            android:exported="true" />
        <activity android:name=".Bed" />
        <activity android:name=".shareActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".full_Medicine" />
        <activity android:name=".find_Meadicine" />
        <activity android:name=".meals_Activity" />
        <activity android:name=".online_Doctor" />
        <activity android:name=".Oxygen" />
        <activity android:name=".LoginAdmin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>